# Quick Start

This guide will help you get up and running with the Modern React Template.

## Prerequisites

- Node.js 20 or higher
- npm (comes with Node.js)

## Installation

```bash
# Clone the repository
git clone <your-repo-url>
cd modern-react-template-lite

# Install dependencies
npm install

# Start development server
npm run dev
```

Visit `http://localhost:5173` to see your application.

## Next Steps

- Review the README for feature configuration and advanced usage
- Explore the `src/` directory for code samples and structure
- Run tests with `npm run test`
- Lint and format code with `npm run lint` and `npm run prettier`

For more information, see the main README.
