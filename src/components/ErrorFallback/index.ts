/**
 * ErrorFallback Module
 *
 * Re-exports the ErrorFallback component and its types for cleaner imports.
 * Follows the project convention of using named exports only.
 *
 * @example
 * ```tsx
 * import { ErrorFallback } from './components/ErrorFallback';
 * import type { ErrorFallbackProps } from './components/ErrorFallback';
 * ```
 */

export { ErrorFallback } from './ErrorFallback';
