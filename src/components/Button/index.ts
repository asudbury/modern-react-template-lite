/**
 * Button Module
 *
 * Re-exports the Button component and its types for cleaner imports.
 * Follows the project convention of using named exports only.
 *
 * @example
 * ```tsx
 * import { Button } from './components/Button';
 * import type { ButtonProps } from './components/Button';
 * ```
 */

export { Button } from './Button';
export type { ButtonProps } from './Button';
