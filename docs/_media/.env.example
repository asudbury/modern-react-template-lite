# ============================================================================
# Modern React Template - Environment Variables
# ============================================================================
# This file documents all environment variables used by the application and
# CI/CD workflows. Copy this file to `.env` for local development.
#
# For forks: Most optional features are disabled by default and don't require
# configuration unless you want to use them.
# ============================================================================

# ----------------------------------------------------------------------------
# REQUIRED: Application Configuration (Vite runtime)
# ----------------------------------------------------------------------------
# These variables are prefixed with VITE_ to be exposed to the client runtime.

# API endpoint for backend
VITE_API_URL=https://api.example.com

# TanStack Query configuration (client runtime)
# Default query stale time in milliseconds (e.g., 300000 = 5 minutes).
VITE_QUERY_STALE_TIME_MS=1000 * 60 * 5

# Whether queries should refetch on window focus. Use "true" or "false".
VITE_QUERY_REFETCH_ON_WINDOW_FOCUS=false

# ----------------------------------------------------------------------------
# OPTIONAL: Feature Flags (Vite runtime)
# ----------------------------------------------------------------------------

# Example feature flag
VITE_FEATURE_X=true

# Analytics key (don't commit real secrets!)
VITE_ANALYTICS_KEY=your-analytics-key-here

# ----------------------------------------------------------------------------
# OPTIONAL: Local Development Settings
# ----------------------------------------------------------------------------

# Skip commit message linting in Husky hook (for temporary use only)
# This is read by .husky/commit-msg, not by Vite
SKIP_COMMITLINT=false

# Enable GitHub Pages artifacts locally (dist/gh-pages-index.html)
# Usually only needed when testing Pages build locally
ENABLE_GH_PAGES=false

# Enable JSDoc/TypeDoc builds locally (matches CI behavior)
# Set to true if you want to test CI documentation builds locally
ENABLE_JSDOC_BUILD=false

# ----------------------------------------------------------------------------
# OPTIONAL: SonarCloud Configuration (used by CI workflows only)
# ----------------------------------------------------------------------------
# ⚠️ FOR FORKS: You can safely ignore this section unless you want to use
# SonarCloud. The workflow won't run without proper GitHub configuration.
#
# To enable SonarCloud:
# 1. Sign up at https://sonarcloud.io
# 2. Set GitHub repository variables (Settings → Secrets and variables → Actions → Variables):
#    - RUN_SONARCLOUD=true
#    - SONAR_ORGANIZATION=your-sonar-organization
#    - SONAR_PROJECT_KEY=your-sonar-project-key
# 3. Set GitHub repository secret (Settings → Secrets and variables → Actions → Secrets):
#    - SONAR_TOKEN=your-sonar-token-here
# ----------------------------------------------------------------------------

# SonarCloud organization key (from SonarCloud dashboard)
SONAR_ORGANIZATION=your-sonar-organization

# SonarCloud project key (from SonarCloud dashboard)
SONAR_PROJECT_KEY=your-sonar-project-key

# ⚠️ NEVER commit this! Use GitHub Secrets instead.
SONAR_TOKEN=your-sonar-token-here

# Control whether SonarCloud workflow runs in CI
# Set to "false" (or don't set) to disable SonarCloud analysis
RUN_SONARCLOUD=false

# ----------------------------------------------------------------------------
# OPTIONAL: GitHub Actions Variables (set in GitHub repository settings)
# ----------------------------------------------------------------------------
# These are typically set in GitHub: Settings → Secrets and variables → Actions → Variables
# They're documented here for reference but should NOT be in your local .env file.
#
# All optional features are DISABLED by default for fork-friendliness.
#
# GitHub Pages deployment:
#   ENABLE_GH_PAGES=true               # Enable Pages workflow
#   ENABLE_JSDOC_BUILD=true            # Include API docs in Pages
#
# SonarCloud analysis:
#   RUN_SONARCLOUD=true                # Enable SonarCloud workflow
#   SONAR_ORGANIZATION=your-org        # Your SonarCloud organization
#   SONAR_PROJECT_KEY=your-key         # Your SonarCloud project key
#   SONAR_TOKEN=secret                 # Add as GitHub Secret (not variable!)
#
# ----------------------------------------------------------------------------
